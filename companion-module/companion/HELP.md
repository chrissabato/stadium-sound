# Stadium Sound

Enable **OSC & iPad Remote** in Stadium Sound's Settings. Enter the computer's IP address, OSC UDP port, remote web port, and the `token` value from the displayed iPad URL.

The module loads banks and tracks from Stadium Sound so they appear as action dropdowns. It provides playback, bank selection, stop, fade, random, and master-volume actions plus active-track and selected-bank feedback.
